[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nolane-ui-intelligence"
version = "0.1.0"
description = "Universal Design Cognition & Verification System for AI agents"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Nolane-x"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
