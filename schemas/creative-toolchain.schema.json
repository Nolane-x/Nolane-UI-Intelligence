{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["goal", "stages", "fallback"],
  "properties": {
    "goal": {"type": "string"},
    "stages": {"type": "array", "minItems": 1, "items": {"type": "object"}},
    "fallback": {"type": "string"}
  }
}
