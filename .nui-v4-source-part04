acks a governed way to discover current external UI primitives, animated components, motion engines, specialist SDKs and design-agent skills. A static list of links is insufficient: sources have different abstraction roles, drift, license terms, accessibility posture, dependencies and integration risks.

## Design principles

1. **Typed source roles, never one library leaderboard.** Animated galleries, motion engines, headless primitives, design systems, editors, data grids, canvas SDKs, 3D renderers and agent-skill catalogues are not interchangeable.
2. **Primary provenance.** Material decisions cite canonical primary URLs and inspected files/docs.
3. **Adoption is a decision, not retrieval.** Retrieval returns candidates; selection chooses `adopt/adapt/inspire/build/reject`.
4. **No popularity authority.** Stars/trending may aid discovery but may not justify a decision.
5. **High-drift means live verification.** Registry data is a cache.
6. **Mechanism over skin.** Inspiration extracts a mechanism and records the adaptation boundary rather than copying a foreign product identity.
7. **Local proof supersedes upstream demos.** Accessibility, behavior, hydration and performance are verified after integration.
8. **Rich interaction is stateful behavior.** Motion/direct manipulation owns interruption, modality equivalence, reduced motion, semantic commit, focus, SSR/hydration, performance, cleanup and exit strategy.

## Six decision owners

- `researching-ui-implementation-ecosystems` → `ui-ecosystem-query`
- `selecting-ui-building-blocks` → `ui-source-selection`
- `adapting-external-ui-patterns` → `ui-adaptation-contract`
- `engineering-rich-interactive-components` → `rich-interaction-contract`
- `auditing-ui-library-integration` → `ui-integration-audit`
- `maintaining-ui-resource-registry` → `ui-resource-registry-delta`

Discovery may not silently select. Selection may not silently adapt. Adaptation may not self-certify integration. Registry maintenance is separate from task-local research.

## Registry model

Each entry records ID/name/canonical URL, source role, categories, capabilities, stacks/platforms, allowed intents, license status/evidence, accessibility posture, drift, live-verification flag, provenance, when-to-use and when-not-to-use. No third-party implementation code is embedded.

The query engine ranks capability, stack, category, source-role and accessibility fit. It returns `live_search_required` when no adequate current candidates exist or the leading candidates all require fresh verification.

## Evidence contracts

A material reference ledger records source URL, usage, mechanism, inspected paths/docs and adaptation boundary. Adopt/adapt decisions require verified task-specific license posture and implementation inspection. Integration audit checks license, dependency, accessibility, reduced motion, SSR/hydration, performance, API drift, security, exit strategy and local runtime.

## Hard routing

External or named source → research + selection. `adapt` → adaptation + integration audit. `adopt` → integration audit. Rich interaction → rich-interaction engineering; if external code participates, integration audit is also mandatory.

## Falsification

Fourteen adversarial cases target popularity-only selection, unresolved license, screenshot-only inspection, stale API, wrong stack, wrong source role, copy-without-adaptation, missing reduced motion, drag without keyboard, hydration mismatch, dependency overkill, missing exit strategy, missing citation and upstream-demo/local-wrapper evidence confusion.

## Bounds

The registry cannot be permanently exhaustive and does not certify any source for every use. Live legal/license review may be necessary for real commercial decisions. NUI's deterministic gates can ensure evidence is present and internally coherent; they cannot guarantee external repositories remain unchanged after verification.
NUIV4FILE	100644	6314	evals/v4/ecosystem/cases.json
{
  "version": 4,
  "suite": "ui-ecosystem-adversaries",
  "cases": [
    {
      "id": "v4-popularity-only",
      "failure": "Choosing React Bits only for GitHub stars, without comparing mechanism, stack, accessibility, license or integration cost",
      "required_skills": [
        "researching-ui-implementation-ecosystems",
        "selecting-ui-building-blocks"
      ],
      "must_find": [
        "Reject popularity-only selection",
        "Compare source role and product fit"
      ]
    },
    {
      "id": "v4-unresolved-license",
      "failure": "Copying an attractive public component without reading the current license or terms",
      "required_skills": [
        "selecting-ui-building-blocks",
        "auditing-ui-library-integration"
      ],
      "must_find": [
        "Block adopt or adapt until license posture is resolved",
        "Require primary license evidence"
      ]
    },
    {
      "id": "v4-screenshot-only-inspection",
      "failure": "Approving a component after gallery screenshots only, with no documentation, dependencies or implementation inspection",
      "required_skills": [
        "researching-ui-implementation-ecosystems",
        "selecting-ui-building-blocks"
      ],
      "must_find": [
        "Screenshots are insufficient evidence",
        "Inspect documentation and implementation before selection"
      ]
    },
    {
      "id": "v4-stale-api",
      "failure": "Memory recommends an old major-version animation API without live verification",
      "required_skills": [
        "maintaining-ui-resource-registry",
        "researching-ui-implementation-ecosystems"
      ],
      "must_find": [
        "Require live verification for high-drift APIs",
        "Treat stale registry knowledge as non-current implementation truth"
      ]
    },
    {
      "id": "v4-wrong-stack",
      "failure": "A React-only component is selected for native Swift because the screenshot looks right",
      "required_skills": [
        "selecting-ui-building-blocks",
        "researching-ui-implementation-ecosystems"
      ],
      "must_find": [
        "Reject adopt or adapt for incompatible stack",
        "Use mechanism inspiration only where transferable"
      ]
    },
    {
      "id": "v4-wrong-source-role",
      "failure": "An animated gallery is used as the sole foundation for dialog focus and keyboard semantics",
      "required_skills": [
        "selecting-ui-building-blocks",
        "auditing-ui-library-integration"
      ],
      "must_find": [
        "Detect source-role mismatch",
        "Route semantics to an appropriate primitive or local contract"
      ]
    },
    {
      "id": "v4-copy-without-adaptation",
      "failure": "A copied card keeps foreign radius colors typography hover motion and demo labels, creating a visual and semantic island",
      "required_skills": [
        "adapting-external-ui-patterns",
        "architecting-component-systems"
      ],
      "must_find": [
        "Map external styling and events to local tokens actions and content",
        "Preserve useful mechanism rather than foreign surface identity"
      ]
    },
    {
      "id": "v4-motion-no-reduced",
      "failure": "An animated background and zoom transition ship with no reduced-motion alternative",
      "required_skills": [
        "engineering-rich-interactive-components",
        "auditing-ui-library-integration"
      ],
      "must_find": [
        "Fail motion integration without reduced-motion behavior",
        "Provide an alternative preserving information without vestibular motion"
      ]
    },
    {
      "id": "v4-drag-no-keyboard",
      "failure": "A sortable dashboard supports drag and drop only despite a keyboard-access obligation",
      "required_skills": [
        "engineering-rich-interactive-components",
        "critiquing-accessibility"
      ],
      "must_find": [
        "Treat drag-only operation as incomplete",
        "Provide keyboard-equivalent reordering"
      ]
    },
    {
      "id": "v4-ssr-hydration",
      "failure": "An animation reads layout during initial render and causes server/client markup mismatch and visual jumps",
      "required_skills": [
        "engineering-rich-interactive-components",
        "auditing-ui-library-integration"
      ],
      "must_find": [
        "Identify SSR and hydration contract risk",
        "Define server fallback or client-only measurement boundary"
      ]
    },
    {
      "id": "v4-dependency-overkill",
      "failure": "A decorative text effect adds a second motion runtime and large transitive dependencies while an equivalent engine already exists",
      "required_skills": [
        "selecting-ui-building-blocks",
        "auditing-ui-library-integration"
      ],
      "must_find": [
        "Compare experiential leverage against dependency duplication",
        "Prefer existing engine or smaller adaptation when equivalent"
      ]
    },
    {
      "id": "v4-no-exit-strategy",
      "failure": "A core editor is tightly coupled to a large SDK with no adapter boundary migration plan or acknowledged lock-in",
      "required_skills": [
        "auditing-ui-library-integration",
        "selecting-ui-building-blocks"
      ],
      "must_find": [
        "Treat lifecycle lock-in as integration risk",
        "Require replacement boundary or explicit authorized lock-in"
      ]
    },
    {
      "id": "v4-citation-missing",
      "failure": "A distinctive mechanism learned from a repository is used without source URL inspected files or provenance",
      "required_skills": [
        "researching-ui-implementation-ecosystems",
        "adapting-external-ui-patterns"
      ],
      "must_find": [
        "Require a material reference-ledger citation",
        "Record mechanism and adaptation boundary"
      ]
    },
    {
      "id": "v4-library-demo-not-local-proof",
      "failure": "An upstream demo is keyboard accessible but the local wrapper changes focus behavior; the team trusts th