# UX Intelligence v2 RED Phase

Contract tests for the canonical bridge, provenance ledger, journey model, verifier, public API/MCP integration, semantic quality court, and status surface were added before production implementation.

The expected RED condition is unresolved imports/surfaces under `nolane_ui.ux_intelligence` until v2 production modules are added.
